{
  "name": "example",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:simple": "CUCUMBER_OPTIONS=\"--tags @simple\" vitest run"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "@lotun/vitest-cucumber": "^0.2.7",
    "vitest": "^4.1.8"
  }
}
