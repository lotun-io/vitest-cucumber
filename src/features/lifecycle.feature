Feature: Lifecycle




# This feature intentionally has no scenarios. It is used internally by the
# plugin to drive Cucumber's BeforeAll / AfterAll test-run hooks in isolation
# (once per worker) without running any scenarios.