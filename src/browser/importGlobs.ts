// src/browser/importGlobs.ts  ← real file, placeholder content
// Content injected at build time by the browserCucumber plugin.
// Load support files in browser mode via import.meta.glob() to ensure they are included in the bundle.
